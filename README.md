# CodeAlpha FAQ Chatbot (Task 2)

A simple FAQ chatbot built with NLTK preprocessing and TF-IDF + Cosine Similarity matching, with a Streamlit chat UI.

## Setup
```
pip install -r requirements.txt
streamlit run app.py
```

Note: Sample university admissions FAQ data used for demo purposes only.
